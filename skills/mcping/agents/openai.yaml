interface:
  display_name: "mcping"
  short_description: "代跑 mcping：Java/Bedrock 测活"
  default_prompt: "按 $mcping 当操作员：用户要 ping Minecraft 服、看 MOTD/延迟/人数时，用 shell 直接跑 mcping（--show / -c / --java / --bedrock 按需），不要只拼命令或 SAMPLE。缺 host 再问一句然后继续跑。"
policy:
  allow_implicit_invocation: true
